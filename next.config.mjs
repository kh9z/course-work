/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "images.unsplash.com", "files.edgestore.dev"]
    }
};

export default nextConfig;
